# anysec-frontend (Svelte)

Build image using: `docker build -t anysec-frontend .`

In a proxy environment, use build arguments:

```bash
docker build --build-arg https_proxy=$(HTTP_PROXY) -t anysec-frontend .
```
